Run 3 of 3 of the technical-accuracy and consistency audit. Scope: Exporting (overview, Lacerte, CCH Axcess, external document), Security (data protection, compliance, AI and data), and Help (FAQ). Code source of truth: `Rialto-Financial/rialto`.

## Changes applied

### exporting/overview.mdx
No changes needed. Page already reflects the correct framing for all three live destinations:
- Lacerte described as direct-import via Data Conductor.
- CCH Axcess described with both routes (direct API integration and manual `.dat` file import).
- GoSystem framed as an Excel-based deliverable for use within GoSystem workflows, explicitly *not* a direct tax-software import.
- No mention of UltraTax or Drake.
- No links to the deleted `/reviewing-data/confidence-scores` page.

### exporting/lacerte.mdx

| Before | After | rialto source |
| --- | --- | --- |
| Field labelled "Client No." in the export configuration steps | "Client Locator" — matches the actual UI label | `apps/web/src/components/exports/destinations/lacerte/*` (destination form field) |
| Stale absolute URL `https://byron-bb7c63f4.mintlify.app/reviewing-data/review-process` | Relative link `/reviewing-data/review-process` | n/a (link hygiene) |

The Data Conductor warning (must click Transfer; must not delete staging area data after Transfer) and the "Best practice: make corrections in Byron, not Lacerte" section with the $50 / $50,000 / $50,050 example were both preserved verbatim.

### exporting/cch-axcess.mdx
No changes needed. Page already documents both export methods (direct API integration and manual `.dat` import through Return Manager) with a Part A / Part B structure. No UltraTax/Drake mentions, no broken links.

### exporting/external-document.mdx

| Before | After | rialto source |
| --- | --- | --- |
| Two `## PDF export` sections (a duplicate had crept in) plus a near-duplicate "When to use Excel" intro paragraph | Single PDF export section after Excel; single Excel intro. PDF section keeps "collated PDF today, bookmarked PDF in development" framing. | n/a (content cleanup) |

The Note inviting firms to email `support@usebyron.com` about custom Excel templates was preserved.

### security/data-protection.mdx

| Before | After | rialto source |
| --- | --- | --- |
| Absolute URL `https://byron-bb7c63f4.mintlify.app/security/compliance` in the "Independent verifications" card | Relative link `/security/compliance` | n/a (link hygiene) |

Page already states SOC 2 Type 1 and Type 2 are both completed, does not name the auditor, and adds no technical-protocol claims beyond what was there.

### security/compliance.mdx

| Before | After | rialto source |
| --- | --- | --- |
| Phrasing implied SOC 2 Type 2 was a future or in-flight effort (e.g. "verification in 2026", `hourglass-half` icon) | Updated to completed-state language. Type 2 is described as completed; icon swapped to `circle-check` to match. | n/a (factual update — Type 2 completion confirmed in the brief) |
| Auditor named ("Insight Assurance") in customer-facing copy | "an independent third-party auditor" | n/a (per audit convention) |

### security/ai-and-data.mdx

| Before | After | rialto source |
| --- | --- | --- |
| Card said SOC 2 Type 2 was "in progress" | Reworded to reflect Type 2 as completed | n/a (factual update) |
| Link to the deleted `/reviewing-data/confidence-scores` page | Repointed to `/reviewing-data/review-screen` (the new home for confidence-level guidance) | docs.json nav |

### help/faq.mdx

| Before | After | rialto source |
| --- | --- | --- |
| "Coming soon: UltraTax CS / Drake" callout | Removed entirely. UltraTax and Drake are disabled in the destination registry and not to be mentioned. | `apps/web/src/components/exports/destinations/registry.ts` (UltraTax and Drake entries flagged `disabled: true`) |
| GoSystem described as a tax-software export alongside Lacerte and CCH Axcess | Reframed as an Excel-based file structured for use within GoSystem — explicitly not a direct tax-software import | same registry |
| CCH Axcess Q\&A described only the manual `.dat` import flow | Updated to mention both methods (direct API integration and manual `.dat` import via Return Manager) | `apps/web/src/components/exports/destinations/cch-axcess/*` |
| Link to deleted `/reviewing-data/confidence-scores` | Repointed to `/reviewing-data/review-screen` | docs.json nav |
| SOC 2 Type 2 referred to as in-progress | Updated to completed-state language consistent with the security pages | n/a |

## Cross-page consistency fixes

- **SOC 2 Type 2 status.** All three security pages (`data-protection`, `compliance`, `ai-and-data`) and `help/faq` now describe Type 2 as completed. No "in progress" or "underway" wording remains.
- **GoSystem framing.** `exporting/overview` and `help/faq` consistently describe GoSystem as an Excel-based deliverable for use within GoSystem workflows, distinct from the direct-import destinations (Lacerte, CCH Axcess).
- **CCH Axcess dual-method.** `exporting/overview`, `exporting/cch-axcess`, and `help/faq` all mention both the direct API integration and the manual `.dat` file import.
- **No UltraTax/Drake.** All "Coming Soon" callouts for UltraTax and Drake removed across Run 3 pages.
- **Auditor name.** Customer-facing security pages refer to "an independent third-party auditor" rather than naming Insight Assurance.
- **confidence-scores link cleanup.** All references on Run 3 pages to the deleted `/reviewing-data/confidence-scores` page now point to `/reviewing-data/review-screen` (which contains the Confidence levels accordion).

## Flagged for human review

1. **UI button label "Initiate Export".** The audit brief lists "Initiate Export" as the canonical label, but the rialto code renders the export-action button dynamically as "Export" or "Export N Document(s)" (no occurrence of "Initiate Export" in the codebase). Per "trust the code," this is a label mismatch on `exporting/cch-axcess.mdx` and `help/faq.mdx`. Left as-is because the brief explicitly listed "Initiate Export" — flagging so a human can decide whether the docs or the code is the source of truth here.
2. **CCH deliverable label "Create CCH Tax File".** The brief lists both "CCH Tax File (.dat)" and "Create CCH Tax File" as canonical labels. In the rialto destination registry, the deliverable label is "CCH Tax File (.dat)"; "Create CCH Tax File" does not appear. `exporting/cch-axcess.mdx` currently instructs users to "Select **Create CCH Tax File** as the export type." Left as-is for the same reason as item 1.
3. **Lacerte category list rendering.** `exporting/lacerte.mdx` Step 5 splits "Series Prefix" into two list items ("Series" and "Prefix"). I could not verify against rialto whether this is one column or two. Did not change.
4. **Hidden-tab links.** No visible Run 3 page links to any of the hidden `future-documents/*` pages, so nothing to flag here.

## Pages skipped (per scope)

Out-of-scope placeholders (under 500 bytes):

- `exporting/gosystem.mdx`
- `help/contact-support.mdx`
- `changelog/release-notes.mdx`
- `changelog/upcoming-features.mdx`

Hidden tab — not modified or audited:

- `future-documents/from-upload-to-ready-for-review.mdx`
- `future-documents/uploading-docs.mdx`
- `future-documents/org-setup.mdx`
