Run 3 of 3 of the accuracy and consistency audit. Scope: 8 pages across exporting, security, and help. Source of truth: Rialto-Financial/rialto.

## Changes applied

### exporting/lacerte.mdx
- Configure-export step: changed "Enter the Client No." to "Enter the Client Locator" to match the UI. Source: apps/frontend/src/components/ExportModal/constants.ts (Lacerte clientLocator.locatorLabel is "Client Locator").
- Replaced an absolute Review-process link with the relative /reviewing-data/review-process.

### exporting/external-document.mdx
- Removed a duplicate PDF export section and the duplicate custom-Excel-template Note that preceded it.
- "When to use Excel": dropped the repeated intro paragraph and replaced it with a short "Use Excel when:" lead-in. Bullets unchanged.

### security/compliance.mdx
- "SOC 2 Type 2 — completed" Card icon changed from hourglass-half to circle-check.
- Reworded "Byron completed a SOC 2 Type 2 verification in 2026" to "Byron has completed a SOC 2 Type 2 examination, conducted by an independent third-party auditor." Type-1-vs-Type-2 explanation kept.

### security/ai-and-data.mdx
- Repointed /reviewing-data/confidence-scores (deleted page) to /reviewing-data/review-screen, where the confidence-levels accordion now lives.
- Compliance Card description: "SOC 2 Type 1, the Type 2 in progress, and the ongoing audit program" replaced with "Our completed SOC 2 Type 1 and Type 2 reports and the ongoing audit program."

### help/faq.mdx
- Removed the "Coming soon: UltraTax CS / Drake" callout from the integrations question. Code shows both as disabled; per audit instructions they should not be mentioned.
- Reframed the GoSystem line so it is no longer presented as a direct tax-software import — it is an Excel-based file structured for use within GoSystem workflows.
- Reframed the CCH Axcess line to mention both export routes (manual .dat import through Return Manager AND the direct API integration), matching exporting/cch-axcess.mdx.
- Rewrote "How does the CCH Axcess integration work?" to describe both methods (API + manual) with the same Byron-side setup.
- Aligned the CCH FAQ button copy from "click Create export" to "click Initiate Export" to match the rest of the exporting docs.
- Repointed /reviewing-data/confidence-scores (deleted page) to /reviewing-data/review-screen.

### Pages with no changes needed
- exporting/overview.mdx, exporting/cch-axcess.mdx, security/data-protection.mdx — read end-to-end and required no changes. They already reflect: SOC 2 Type 2 complete, GoSystem framed correctly, CCH dual-method described, no UltraTax/Drake mentions, no auditor named.

## Cross-page consistency fixes

- SOC 2 Type 2 status. All three security pages and the FAQ now treat SOC 2 Type 2 as completed. Stale "in progress" wording removed; the compliance Type 2 Card icon is no longer the in-progress hourglass.
- GoSystem framing. exporting/overview.mdx already describes GoSystem as an Excel-based deliverable that is NOT a direct tax-software import. The FAQ now matches.
- CCH dual-method. exporting/overview.mdx and exporting/cch-axcess.mdx already cover both the API integration and the manual .dat import. The FAQ has been rewritten to match.
- No UltraTax or Drake. Removed the only "Coming soon" callout (in faq). Other Run 3 pages already had no UltraTax/Drake references.
- Deleted confidence-scores page. All Run 3 references repointed to /reviewing-data/review-screen. mint broken-links confirms no Run 3 page links to the deleted path.
- Auditor not named. None of the Run 3 security pages name the auditor; they consistently say "an independent third-party auditor."

## Flagged for human review

1. Button label "Initiate Export". The audit instructions list it as a canonical UI label. The product code (packages/ui/src/components/ExportModal/steps/SelectDocumentsStep.tsx) currently renders the primary button as "Export N Document(s)" or just "Export", not "Initiate Export". Docs left unchanged on the assumption this is intentional product copy that the code will catch up to.
2. CCH deliverable label "Create CCH Tax File". The audit instructions list it as canonical. The product code (apps/frontend/src/components/ExportModal/constants.ts) renders the deliverable as "CCH Tax File (.dat)". The docs in exporting/cch-axcess.mdx say "Select Create CCH Tax File as the export type." Left unchanged — flagging in case the deliverable label should be "CCH Tax File (.dat)" everywhere.
3. Lacerte category list. exporting/lacerte.mdx step 5 lists 7 categories with "Series" and "Prefix" as separate entries; industry usage suggests "Series Prefix" is one category. Could not verify against rialto (the Data Conductor profile config lives outside Byron). Left as-is.
4. exporting/overview.mdx "What's next" links to /exporting/gosystem, which is an intentional under-500-byte placeholder. Left unchanged per scope rules.
5. FAQ "Is my data secure when integrating with CCH or Lacerte?" still mentions OAuth 2.0, TLS 1.3, and IRS Publication 4557. The "do not add specific protocol versions / technical claims" rule was scoped to the security pages, so this FAQ content was preserved. Flagging in case these should be softened in line with the security pages.
6. Hidden future-documents/* links from visible Run 3 pages. None found.

## Pages skipped

- Hidden tab pages: future-documents/from-upload-to-ready-for-review, future-documents/uploading-docs, future-documents/org-setup — out of scope.
- Placeholder pages (under 500 bytes): exporting/gosystem, help/contact-support, changelog/release-notes, changelog/upcoming-features — not audited per scope rules.
- All other docs pages — outside Run 3 scope.

## Validation

- mint validate — passes.
- mint broken-links — zero broken links in Run 3 pages. Reported broken links are all in hidden future-documents/* and getting-started/welcome-page.mdx, all outside Run 3 scope.