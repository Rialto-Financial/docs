---
title: "CCH Axcess"
description: "Export Byron's extracted data into CCH Axcess via direct API integration or `.dat` file import."
---

Byron exports to CCH Axcess as a `.dat` file. There are two ways to get that file into CCH Axcess: through Byron's direct API integration with CCH (no manual file handling required), or by downloading the `.dat` file from Byron and importing it manually through Return Manager. Both methods use the same underlying file — your firm picks whichever fits your setup.

<Note>
  If you run into problems during the export or import, reach out to [support@usebyron.com](mailto:support@usebyron.com).
</Note>

## Two export methods

<CardGroup cols={2}>
  <Card title="API integration" icon="bolt">
    Byron pushes the export directly to CCH Axcess through the API. There's no file to download and no manual import — once you initiate the export from Byron, the data lands in CCH Axcess on its own.
  </Card>

  <Card title="Manual file import" icon="file-arrow-down">
    Byron generates a `.dat` file that you download and import through CCH Axcess Return Manager yourself. Useful for firms that aren't on the API integration, or as a fallback if the API isn't available.
  </Card>
</CardGroup>

The rest of this page walks through both flows. The setup in Byron (Part A) is the same regardless of which method you use — the difference is what happens after you click **Initiate Export**.

## Before you start

Every document you want to export must be in **Approved** status. If anything in the filing is still in review, finish approving it first. See [Review process](/reviewing-data/review-process) for how to approve documents.

## Exporting a filing into CCH Axcess

### Part A — configure and initiate the export from Byron

This part is the same for both export methods.

<Steps>
  <Step title="Open the filing and create an export">
    From the client's filing page, click **Create an Export** in the top-right corner.
  </Step>
  <Step title="Configure the export">
    In the Configure Export dialog:

    - Select **CCH Axcess** as the destination.
    - Enter the **Client ID** — this must match the client ID in CCH Axcess.
    - Select **Create CCH Tax File** as the export type.
    - Choose which documents to include.
  </Step>
  <Step title="Initiate the export">
    Click **Initiate Export** at the top or bottom of the dialog to start generating the `.dat` file.
  </Step>
</Steps>

What happens next depends on your firm's setup:

- **If your firm is on the API integration**, Byron pushes the export directly into CCH Axcess. You don't need to download anything or open Return Manager — the data arrives on its own. You can skip the rest of this page.
- **If your firm is on manual file import**, the `.dat` file is available to download from Byron. Continue with Part B below.

### Part B — manual file import (skip if you're on the API integration)

<Steps>
  <Step title="Download the file from Byron">
    The file usually takes 30 to 90 seconds to generate. When it's ready, click the **CCH Tax File (.dat)** link to download.
  </Step>
  <Step title="Save the file">
    Save the `.dat` file somewhere you can find from CCH Axcess. The default Downloads folder is fine.
  </Step>
</Steps>

<Warning>
  Before you start the import, make sure the return is **not currently open** in CCH Axcess. The import will fail if Return Manager has the return loaded.
</Warning>

<Steps>
  <Step title="Open Import in Return Manager">
    In CCH Axcess Return Manager, click the **Utilities** menu, then select **Import** under the **Tax Data** section.

    <Frame>
      ![cch-step7-CL6n2JMi](/images/cch-step7-CL6n2JMi.png)
    </Frame>
  </Step>
  <Step title="Browse for the .DAT file">
    In the Import Tax Data dialog, click **Browse...** and navigate to the `.dat` file you downloaded from Byron.

    <Frame>
      ![cch-step8-DnmCIMdU](/images/cch-step8-DnmCIMdU.png)
    </Frame>
  </Step>
  <Step title="Validate and import">
    Click **Validate** to verify the file. Once validation completes successfully, click **Import** to process the data, then click **Finish**.

    <Frame>
      ![cch-step9a-DynhVmFD](/images/cch-step9a-DynhVmFD.png)
    </Frame>

    <Frame>
      ![cch-step9b-BlGdDm3B](/images/cch-step9b-BlGdDm3B.png)
    </Frame>

    <Frame>
      ![cch-step9c-DO52UjUA](/images/cch-step9c-DO52UjUA.png)
    </Frame>
  </Step>
</Steps>

<Tip>
  After the import finishes, it usually takes a few seconds to a minute for the return to appear in Return Manager. Click **Refresh Grid** if you don't see it right away.
</Tip>

## What's next

<CardGroup cols={2}>
  <Card title="Export overview" icon="file-export" href="/exporting/overview">
    Compare destinations and see what travels with every export.
  </Card>

  <Card title="Download as Excel or PDF" icon="file-arrow-down" href="/exporting/external-document">
    Use Byron's Excel workpaper or collated PDF outside of a tax package.
  </Card>
</CardGroup>